# AIoT Digital Twin Dashboard (Flask GUI)

A lightweight Flask-based GUI for an AIoT + Digital Twin + Maintenance workflow dashboard.

## Quick start

```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
python app.py
```

Open `http://localhost:5000` to view the dashboard.
